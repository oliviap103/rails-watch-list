
  <h1 class="d-flex justify-content-center m-3 fs.1">Watch Lists</h1>

  <h3><%= link_to "Create A New List", new_list_path %></h3>

  <% @lists.each do |list| %>
    <%= link_to list_path(list) do %>
        <div class="card">
            <%= list.name %>
        </div>
    <% end %>
<% end %>
